FROM alpine:edge

RUN apk add --no-cache bash postfix postfix-pcre rsyslog ca-certificates gettext

COPY config /root/

EXPOSE 25

WORKDIR /root

RUN /root/config/init-postfix.sh

ENTRYPOINT ["/root/config/entrypoint-postfix.sh"]
CMD ["startup"] 
