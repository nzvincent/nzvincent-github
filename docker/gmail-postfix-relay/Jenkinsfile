pipeline{
  agent any
  
  stages{
    stage("Building docker image"){
       agent {
        docker.build("alpine-postfix:homebrew", "./docker/gmail-postfix-relay/Dockerfile")
      }
    }
    
  
  }
}
