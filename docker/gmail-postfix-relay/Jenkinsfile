pipeline{
  agent any
  
  stages{
    stage("Building docker image"){
      agent{
        docker.image('alpine:edge').withRun('-e test') {
          sh 'env'
        }  
      }
    }
    
  
  }
}
