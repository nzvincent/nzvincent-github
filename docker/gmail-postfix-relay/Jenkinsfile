pipeline {
    agent any
    stages {
        stage('Docker build') {
            steps {
                sh 'docker build -t "alpine-postfix:homebrew" ./docker/gmail-postfix-relay/Dockerfile'    
            }
        }
    }
}
