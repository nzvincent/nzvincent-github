pipeline{
  agent any
  
  stages{
    stage("Building docker image"){
      steps{
        sh 'echo "building docker image"'
        alpine-postfix = docker.build("alpine-postfix:homebrew", "./docker/gmail-postfix-relay/Dockerfile")
      }
    }
    
  
  }
}
