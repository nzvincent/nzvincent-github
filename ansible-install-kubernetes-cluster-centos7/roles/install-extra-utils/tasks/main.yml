---

- block:

  #####################################################
  # Install NFS server
  #####################################################

  - name: Install Docker Registry
    yum:
      name: "{{ packages }}"
      state: latest
    vars:
      packages:
      - nfs-utils 
    ignore_errors: yes  
